{
  "name": "server",
  "version": "1.0.0",
  "description": "This will be a server",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "in": "git init   git add .  git status",
    "push": "git push -u origin main"
  },
  "author": "Maarten",
  "license": "MIT",
  "dependencies": {
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "nedb": "^1.8.0",
    "node-fetch": "^3.3.1"
  }
}
